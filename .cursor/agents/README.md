# Subagents

Subagent definitions live here, one file per agent:

```
.cursor/agents/<agent-name>.md
```

Subagents are **per project**. Add them when a focused, reusable assistant is
useful (e.g. a docs researcher, a site runner). The base template ships none.
See `TEMPLATE.md` §7.3.
